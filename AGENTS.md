# .github 仓库协作补充规范

## 1. 仓库定位

- `.github` 负责维护 HNPlugins 组织级 issue / PR 模板、共享 workflow、仓库入口文档与协作提示。
- 本仓库中的改动会影响多个仓库，因此优先考虑组织级一致性、可落地性与失败提示可读性。

## 2. 文档与入口约束

- 组织级入口文档必须明确指向 `HNProjects/Wiki` 与 `HNPlugins/standards`，不要把业务说明塞进组织入口仓库。
- 若面向 HN 系列下游插件或业务仓库提供提示，必须直白提醒：优先复用 `HNCore` / `HNCoreAPI` / 组织公共能力，禁止重复造轮子。
- shared workflow、模板与入口文案应尽量把“该去哪里看文档、该先复用什么、提交前如何自检”说清楚，避免只留下抽象口号。

## 3. 工作流与模板约束

- reusable workflow 的报错信息要讲人话：指出缺失文件、缺少哪类提示，以及应该补什么。
- 组织级 workflow 默认服务多个仓库，新增规则前要确认不会无故破坏模板仓库、文档仓库或非 Java 项目。
- 若规则会影响各仓库入口文档，应同步评估是否需要更新模板仓库和既有仓库。

## 4. 变更验证

- 提交前必须按 `HNPlugins/standards` 与本仓库规则完成自检，确认入口文档、 workflow 路径与失败提示保持一致。
- 修改共享 workflow 或模板时，至少对代表仓库做本地校验，确认 `AGENTS.md`、`CONTRIBUTING.md` 和关键提示词检查都能通过。
- 不符合团队规范、缺少迁移说明或无法在现有仓库落地的规则改动，不得直接提交。
